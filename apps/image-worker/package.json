{
  "name": "image-worker",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx esbuild ./src/processor.ts --bundle --platform=node --outfile=dist/processor.js",
    "start": "node ./dist/processor.js",
    "dev": "nodemon --exec \"node -r esbuild-register ./src/processor.ts\" -e .ts",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/database": "*",
    "@repo/typescript-config": "*",
    "@repo/aws_s3": "*",
    "amqplib": "^0.10.4",
    "dotenv": "^16.4.5",
    "esbuild": "^0.24.0",
    "esbuild-register": "^3.6.0"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.5",
    "nodemon": "^3.1.4",
    "ts-node": "^10.9.2"
  }
}
